Index: doc/sslio.8.html
===================================================================
RCS file: /var/lib/cvs/ipsvd/doc/sslio.8.html,v
retrieving revision 1.2
retrieving revision 1.3
diff -u -r1.2 -r1.3
--- doc/sslio.8.html	15 May 2004 13:57:36 -0000	1.2
+++ doc/sslio.8.html	21 Jun 2004 16:35:46 -0000	1.3
@@ -9,22 +9,22 @@
 <a href='http://smarden.org/pape/'>G. Pape</a><br><a href='index.html'>ipsvd</a><hr>
 
 <h2><a name='sect0'>Name</a></h2>
-sslio - SSL input/output for service daemons 
+sslio - SSL input/output for service programs 
 <h2><a name='sect1'>Synopsis</a></h2>
 <b>sslio</b> [-u <i>user</i>]
 [-U <i>user</i>] [-/ <i>root</i>] [-C <i>cert</i>] [-K <i>key</i>] [-v] <i>prog</i> 
 <h2><a name='sect2'>Description</a></h2>
 <b>sslio</b> provides SSL
-encrypted network connections for service daemons started by <i><b>tcpsvd</b>(8)</i>,
+encrypted network connections for service programs started by <i><b>tcpsvd</b>(8)</i>,
 or <i><b>tcpserver</b>(1)</i>. <p>
 Normally <b>sslio</b> is started by <i><b>tcpsvd</b>(8)</i> or <i><b>tcpserver</b>(1)</i>,
-in turn starts the service daemon <i>prog</i>, and runs as child process of the
-service daemon. After performing the SSL handshake, <b>sslio</b> reads SSL encrypted
+in turn starts the service program <i>prog</i>, and runs as child process of the
+service program. After performing the SSL handshake, <b>sslio</b> reads SSL encrypted
 data from standard input (the network), and writes decrypted data to the
 standard input of <i>prog</i>; it reads data from the standard output of <i>prog</i>,
 and writes SSL encrypted data to standard output (the network). <p>
 <b>sslio</b> should
-run under a different user ID than the service daemon, and with a changed
+run under a different user ID than the service program, and with a changed
 root directory. When started by root, the -u option must be given, and the
 -U and -/ options should be given. <p>
 The <b>sslio</b> program uses the SSLv3 implementation
@@ -35,7 +35,7 @@
 
 <dt><i>prog</i> </dt>
 <dd><i>prog</i> consists of one or more arguments,
-specifying the service daemon normally run directly by <i><b>tcpsvd</b>(8)</i>, or <i><b>tcpserver</b>(1)</i>.
+specifying the service program normally run directly by <i><b>tcpsvd</b>(8)</i>, or <i><b>tcpserver</b>(1)</i>.
 </dd>
 
 <dt><b>-u <i>user</i>[:<i>group</i>]</b> </dt>
@@ -48,7 +48,7 @@
 
 <dt><b>-U <i>user</i>[:<i>group</i>]</b> </dt>
 <dd>drop permissions. Switch user ID to <i>user</i>&rsquo;s UID,
-and group ID to <i>user</i>&rsquo;s primary GID before starting the service daemon <i>prog</i>.
+and group ID to <i>user</i>&rsquo;s primary GID before starting the service program <i>prog</i>.
 If <i>user</i> is followed by a colon and a group name, the group ID is switched
 to the GID of <i>group</i> instead. All supplementary groups are removed. This option
 should be set when <b>sslio</b> is started by root, and cannot be set otherwise.
Index: man/sslio.8
===================================================================
RCS file: /var/lib/cvs/ipsvd/man/sslio.8,v
retrieving revision 1.2
retrieving revision 1.3
diff -u -r1.2 -r1.3
--- man/sslio.8	15 May 2004 13:57:36 -0000	1.2
+++ man/sslio.8	21 Jun 2004 16:35:47 -0000	1.3
@@ -1,6 +1,6 @@
 .TH sslio 8
 .SH NAME
-sslio \- SSL input/output for service daemons
+sslio \- SSL input/output for service programs
 .SH SYNOPSIS
 .B sslio
 [\-u
@@ -17,7 +17,7 @@
 .I prog
 .SH DESCRIPTION
 .B sslio
-provides SSL encrypted network connections for service daemons started by
+provides SSL encrypted network connections for service programs started by
 .BR tcpsvd (8),
 or
 .BR tcpserver (1).
@@ -28,9 +28,9 @@
 .BR tcpsvd (8)
 or
 .BR tcpserver (1),
-in turn starts the service daemon
+in turn starts the service program
 .IR prog ,
-and runs as child process of the service daemon.
+and runs as child process of the service program.
 After performing the SSL handshake,
 .B sslio
 reads SSL encrypted data from standard input (the network), and writes
@@ -41,7 +41,7 @@
 and writes SSL encrypted data to standard output (the network).
 .P
 .B sslio
-should run under a different user ID than the service daemon, and with a
+should run under a different user ID than the service program, and with a
 changed root directory.
 When started by root, the \-u option must be given, and the \-U and \-/
 options should be given.
@@ -53,7 +53,7 @@
 .TP
 .I prog
 .I prog
-consists of one or more arguments, specifying the service daemon normally
+consists of one or more arguments, specifying the service program normally
 run directly by
 .BR tcpsvd (8),
 or
@@ -83,7 +83,7 @@
 .IR user 's
 UID, and group ID to
 .IR user 's
-primary GID before starting the service daemon
+primary GID before starting the service program
 .IR prog .
 If
 .I user
